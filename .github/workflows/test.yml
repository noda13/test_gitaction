name: learn-github-actions
on:
  push:
    branches:
      - main
jobs:
  check-bats-version:
    runs-on: ubuntu- latest
    steps:
      - name: Run a one-line script
        run: echo "Hello, world!"
